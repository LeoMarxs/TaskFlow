{
  "name": "taskflow-server",
  "version": "1.0.0",
  "description": "TaskFlow Corporate v3 — servidor local com autenticação por senha",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "bcrypt": "^5.1.1"
  }
}